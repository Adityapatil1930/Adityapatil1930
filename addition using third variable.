#include<stdio.h>
#include<conio.h>
void main()
{
  int a,b,c;
  clrscr();
  printf("\/ Enter the first number=");
  scanf("%d",&a);
  printf("\n Enter the second number=");
  scanf("%d",&b);
  c=a+b;
  printf("\n ADDITION=%d",c);
  getch();
}
