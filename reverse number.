#include<stdio.h>
#include<conio.h>
void main()
{
int rem,no,rev=0,sum=0;
clrscr();
printf("\n enter the number=");
scanf("%d",%no);
while(no!=0)
{
rem=no%10;
rev=rev*10+rem;
sum=sum+rem;
no=no/10;
}
printf("\n rev=%d",rev);
printf("\n sum=%d",sum);
getch();
}
