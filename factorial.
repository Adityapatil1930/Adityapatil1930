#include<stdio.h>
#include<conio.h>
void main()
{
int i,fact=1,number;
clrscr();
printf("\n enter the number=");
scanf("%d",&number);
for(i=1;i<=numder;i++)
{
fact=fact*i;
}
printf("\n factorial is :%d",fact);
getch();
}
